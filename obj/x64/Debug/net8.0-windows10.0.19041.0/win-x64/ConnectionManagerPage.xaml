<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Hyper_Transmit.ConnectionManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hyper_Transmit"
    xmlns:models="using:Hyper_Transmit.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <Grid ColumnDefinitions="300,*">

        <!-- Connection List (Left Panel) -->
        <Grid Grid.Column="0" RowDefinitions="Auto,*,Auto" Padding="8">
            <!-- Toolbar -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="4" Margin="0,0,0,8">
                <Button x:ConnectionId='24' x:Name="NewConnectionButton" Content="＋ 新建"                                  />
                <Button x:ConnectionId='25' x:Name="DeleteConnectionButton" Content="删除"                                      IsEnabled="False"/>
            </StackPanel>

            <!-- Connection List -->
            <ListView x:ConnectionId='22'
                Grid.Row="1"
                x:Name="ConnectionList"
                                                                  
                SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="4,6" ColumnDefinitions="24,*,Auto">
                            <FontIcon
                                Grid.Column="0"
                                Glyph="&#xE71B;"
                                FontSize="16"
                                Foreground="{ThemeResource SystemControlForegroundAccentBrush}"
                                VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Margin="8,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="{Binding Username, Mode=OneWay}" FontSize="11"
                                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                            </StackPanel>
                            <FontIcon
                                Grid.Column="2"
                                Glyph="&#xE734;"
                                FontSize="14"
                                Visibility="{Binding IsFavorite, Converter={StaticResource BoolToVisibilityConverter}}"
                                VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Connection Detail / Edit Form (Right Panel) -->
        <ScrollViewer Grid.Column="1" Padding="16,8">
            <Grid>
            <StackPanel x:ConnectionId='2' x:Name="EditPanel" Spacing="12" MaxWidth="600" Visibility="Collapsed">

                <TextBlock Text="连接详情" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>

                <!-- Name -->
                <StackPanel Spacing="4">
                    <TextBlock Text="名称" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    <TextBox x:ConnectionId='21' x:Name="EditNameBox" PlaceholderText="连接名称"/>
                </StackPanel>

                <!-- Host & Port -->
                <Grid ColumnDefinitions="*,8,100">
                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="主机" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBox x:ConnectionId='20' x:Name="EditHostBox" PlaceholderText="hostname or IP"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Spacing="4">
                        <TextBlock Text="端口" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <NumberBox x:ConnectionId='19' x:Name="EditPortBox" Value="22" SpinButtonPlacementMode="Compact"/>
                    </StackPanel>
                </Grid>

                <!-- Protocol & Auth Type -->
                <Grid ColumnDefinitions="*,8,*">
                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="协议" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <ComboBox x:ConnectionId='18' x:Name="EditProtocolCombo" SelectedIndex="0">
                            <ComboBoxItem Content="SFTP"/>
                            <ComboBoxItem Content="SCP"/>
                            <ComboBoxItem Content="FTP"/>
                            <ComboBoxItem Content="FTPS"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Spacing="4">
                        <TextBlock Text="认证方式" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <ComboBox x:ConnectionId='17' x:Name="EditAuthTypeCombo" SelectedIndex="0">
                            <ComboBoxItem Content="密码"/>
                            <ComboBoxItem Content="私钥"/>
                            <ComboBoxItem Content="私钥+密码"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <!-- Username -->
                <StackPanel Spacing="4">
                    <TextBlock Text="用户名" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    <TextBox x:ConnectionId='16' x:Name="EditUsernameBox" PlaceholderText="用户名"/>
                </StackPanel>

                <!-- Password -->
                <StackPanel x:ConnectionId='4' x:Name="EditPasswordSection" Spacing="4">
                    <TextBlock Text="密码" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    <PasswordBox x:ConnectionId='15' x:Name="EditPasswordBox" PlaceholderText="密码（保存时加密存储）"/>
                </StackPanel>

                <!-- Private Key Path -->
                <StackPanel Spacing="4">
                    <TextBlock Text="私钥路径" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBox x:ConnectionId='13' Grid.Column="0" x:Name="EditKeyPathBox" PlaceholderText="私钥文件路径 (.pem)"/>
                        <Button x:ConnectionId='14' Grid.Column="1" Content="浏览..." Margin="4,0,0,0"                              />
                    </Grid>
                </StackPanel>

                <!-- Default Paths -->
                <Grid ColumnDefinitions="*,8,*">
                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="本地默认路径" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBox x:ConnectionId='12' x:Name="EditLocalPathBox" PlaceholderText="如 C:\Users\..."/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Spacing="4">
                        <TextBlock Text="远程默认路径" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBox x:ConnectionId='11' x:Name="EditRemotePathBox" PlaceholderText="/" Text="/"/>
                    </StackPanel>
                </Grid>

                <!-- Favorite -->
                <CheckBox x:ConnectionId='5' x:Name="EditFavoriteCheck" Content="⭐ 添加到收藏"/>

                <!-- Notes -->
                <StackPanel Spacing="4">
                    <TextBlock Text="备注" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    <TextBox x:ConnectionId='10' x:Name="EditNotesBox" PlaceholderText="可选备注" TextWrapping="Wrap" Height="60"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                    <Button x:ConnectionId='7' x:Name="SaveButton" Content="保存" Style="{ThemeResource AccentButtonStyle}"                         />
                    <Button x:ConnectionId='8' x:Name="ConnectButton" Content="一键连接" Style="{ThemeResource AccentButtonStyle}"                            />
                    <Button x:ConnectionId='9' x:Name="TestButton" Content="测试连接"                         />
                </StackPanel>

                <!-- Status -->
                <TextBlock x:ConnectionId='6' x:Name="StatusText" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
            </StackPanel>

            <!-- Placeholder when nothing selected -->
            <StackPanel x:ConnectionId='3' x:Name="PlaceholderPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Visible">
                <FontIcon Glyph="&#xE71B;" FontSize="48" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="选择一个连接进行编辑，或点击新建" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>

